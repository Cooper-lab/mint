"""Data cleaning script for {{ project_name }}."""

import os
import sys
from pathlib import Path
import pandas as pd

# Add src directory to path
sys.path.insert(0, str(Path(__file__).parent))

def main():
    """Main cleaning function."""
    print("Starting data cleaning for {{ project_name }}...")

    # Set up directories
    project_root = Path(__file__).parent.parent
    raw_data_dir = project_root / "data" / "raw"
    intermediate_data_dir = project_root / "data" / "intermediate"

    print(f"Raw data directory: {raw_data_dir}")
    print(f"Intermediate data directory: {intermediate_data_dir}")

    # TODO: Implement data cleaning logic
    # - Load raw data
    # - Clean and preprocess
    # - Save to data/intermediate/

    print("Data cleaning completed.")

if __name__ == "__main__":
    main()